<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidFragmentHW"
        tools:targetApi="31">

        <!-- ของเดิม: MainActivity (ไม่เป็น LAUNCHER ในโปรเจ็กต์นี้) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ของเดิม: เพื่อน (ยังคงไว้ และเป็น LAUNCHER ตามที่โปรเจ็กต์ตั้ง) -->
        <activity
            android:name=".A66102122108Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ของเรา: เพิ่มเข้ามาเฉย ๆ ไม่ต้องเป็น LAUNCHER -->
        <activity
            android:name=".A66102122116Activity"
            android:exported="false" />

    </application>
</manifest>
